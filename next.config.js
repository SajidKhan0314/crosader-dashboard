/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["agile-lowlands-91596.herokuapp.com", "localhost"],
  },
};

module.exports = nextConfig;
